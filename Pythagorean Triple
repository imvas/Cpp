#include<stdio.h>
int main()
{
   long long int a,b,c;
   scanf("%lld",&a);
   if(a%2!=0)
   {
       b=(a*a)/2;
       c=(((a*a)/2)+1);
   }
   else  {
   b=(((a*a)/4)-1);
   c=(((a*a)/4)+1);
   }
   printf("%lld %lld %lld",a,b,c);


}
